#include <iostream>
using namespace std;

int n,k,ans(0);
int *nums;
int mul[100];

int scheduling()
{
	
}

int main()
{
	cin >> n >> k;
	nums = new int[k];
	for (int i=0;i<k;i++)
		cin >> nums[i];
	delete[] nums;
}